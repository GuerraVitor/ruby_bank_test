<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Conta Corrente" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_include_tag "application", "data-turbo-track": "reload", defer: true %>
  </head>

  <body>
    <div class="container-fluid border border-4 rounded-3 p-3 my-3"> <%# This is the main Bootstrap container %>
      <% if flash[:notice] %>
        <div class="flash-message flash-notice">
          <%= flash[:notice] %>
        </div>
      <% elsif flash[:alert] %>
        <div class="flash-message flash-alert">
          <%= flash[:alert] %>
        </div>
      <% end %>

      <%= yield %>
    </div> 
  </body>
</html>